import type { NextConfig } from "next";

// Production is public and indexable. Preview deployments are protected by
// Vercel Authentication at the project level, so no robots header is needed here.
const nextConfig: NextConfig = {};

export default nextConfig;
